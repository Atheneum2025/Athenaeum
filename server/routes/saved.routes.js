// const express = require('express')
// const {createSave, deleteSave, getAllSaves} = require('../controllers/saved.controller');
// const router = express.Router();
// const {verifyJWT} = require("../middlewares/verify.js")

// router.route("/").post(verifyJWT, createSave).get(getAllSaves);
// router.route("/:savedId").delete(deleteSave)

// module.exports = router